server:
  port: ${SERVER_PORT:8881}
  servlet:
    encoding:
      charset: utf-8
      force: true
  error:
    include-exception: true
    include-stacktrace: always
    whitelabel:
      enabled: false

spring:
  mvc:
    # Disable view resolution since the frontend will be served by React. The JSP view resolver can be removed once all JSPs are migrated.
    throw-exception-if-no-handler-found: true
    dispatch-options-request: false
    view:
      prefix: /WEB-INF/view/
      suffix: .jsp
    hiddenmethod:
      filter:
        enabled: true
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB
  datasource:
    url: ${DB_URL:jdbc:mysql://team2-free-project-mysql-db.crwceg2wuy6s.ap-northeast-2.rds.amazonaws.com/greendb}
    username: ${DB_USERNAME:admin}
    password: ${DB_PASSWORD:12345678}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: ${JPA_SHOW_SQL:false}
  sql:
    init:
      mode: ${SQL_INIT_MODE:never}
  web:
    cors:
      allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000}
      allowed-methods: GET,POST,PUT,DELETE
  output:
    ansi:
      enabled: always
  security:
    debug: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: jhjun7338@gmail.com
    password: bmhq dynh jpix nggz
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

gemini:
  api:
    key: AIzaSyDk201IOhtTgnJep5J2UbnWEQlxWZPkXYE

  # Removed mybatis configuration since the project now uses Spring Data JPA

jwt:
  secret: ${JWT_SECRET:myScretKey0000fdafadsdfdsafdasffsafsafasfdfdfsdafafsasdyasdyasdasduasbdhafjkshfbahwefbakvbagkcvadsv}
  expiration: ${JWT_EXPIRATION:36000000}

aws:
  s3:
    bucket-name: ${AWS_S3_BUCKET_NAME:green-front-51217}
    region: ${AWS_REGION:ap-northeast-2}
    access-key: ${AWS_ACCESS_KEY:AKIA5HLHWYDFRNIS7JUC}
    secret-key: ${AWS_SECRET_KEY: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}
    base-url: ${AWS_S3_BASE_URL:https://green-front-51217.s3.ap-northeast-2.amazonaws.com}
